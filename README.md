🔐 File Integrity Monitor (FIM)

File Integrity Monitor (FIM) — десктоп-приложение на C++ для анализа файлов и контроля их целостности.
Программа позволяет загружать произвольные файлы, вычислять их криптографический хеш SHA-256 и подписывать метаданные с помощью HMAC (SHA-256).

Приложение разработано для Linux, использует Qt 6 (Qt Widgets) и распространяется в формате AppImage.

✨ Основные возможности

📂 Загрузка и анализ произвольных файлов

🔐 Вычисление SHA-256 хеша

🧾 Подпись метаданных с использованием HMAC

🕓 Хранение истории сканирований

🔍 Обнаружение изменений файлов и записей БД


💾 Автоматическое сохранение состояния и настроек

🕓 История сканирований

В базе данных сохраняется:

-путь к файлу

-дата и время сканирования

-SHA-256 хеш

-статус целостности

-комментарии и служебные метаданные

История автоматически восстанавливается при следующем запуске приложения.

🔏 Поддержка HMAC

Метаданные файлов подписываются HMAC (SHA-256).
Это позволяет обнаружить:

-подмену данных в базе,

-модификацию истории сканирований,

-несанкционированные изменения метаданных.

Запись считается корректной, если при повторной проверке подпись совпадает.

🧩 Архитектура проекта

История сканирований сохраняется в БД и восстанавливается при следующем запуске.
FIM/
├── core/        # Ядро приложения
├── storage/     # Работа с базой данных
├── gui/         # Графический интерфейс (Qt)
└── main.cpp     # Точка входа

🧠 Ядро приложения (core/)
| Компонент               | Назначение                                                     |
| ----------------------- | -------------------------------------------------------------- |
| **FileIntegrityEngine** | Центральный компонент ядра, управляет процессом сканирования   |
| **FileScanner**         | Постраничное чтение файлов и сбор метаданных                   |
| **FileMetadata**        | Структура метаинформации (путь, размер, владелец, права и др.) |
| **FileStatus**          | Состояния файла: `Ok`, `Changed`, `Error` и др.                |
| **IHasher**             | Абстрактный интерфейс хеширования                              |
| **ScanSummary**         | Краткий отчёт о результатах сканирования                       |
🗄 Работа с базой данных (storage/)
DatabaseManager

Управляет SQLite-базой данных:

-создаёт таблицы при первом запуске,

-контролирует версию схемы,

-выполняет миграции,

-обеспечивает атомарность операций.

Таблицы

files — актуальные данные о файлах

scan_history — история сканирований и изменений

QtStorageAdapter

Адаптер между IStorage и DatabaseManager.
Используется для хранения истории и пользовательских настроек интерфейса.

🖥 Графический интерфейс (gui/)
Компонент	Назначение
MainWindow	Главное окно приложения
FileMonitor	Управление процессом сканирования
ScanWorker	Сканирование в отдельном потоке (QThread)
Notifier	Уведомления (tray)
QtHasher	Реализация SHA-256 через QCryptographicHash
📦 Зависимости

C++17

Qt 6 (Qt Widgets)

SQLite

QCryptographicHash / OpenSSL (HMAC SHA-256)

CMake

AppImageTool

📦 Установка (AppImage)

Скачайте последнюю версию во вкладке Releases

Выдайте права на выполнение:

chmod +x FIM-x.y.z.AppImage


Запустите приложение:

./FIM.AppImage

▶ Использование приложения
📂 Открытие файла

Файл → Открыть файл…

Общая информация о файле и его метаданных.

⚙️ Настройки

Меню → Настройки
Все изменения сохраняются автоматически.

🕓 История

История сканирований сохраняется в БД и восстанавливается при следующем запуске.
