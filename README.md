🔐 File Integrity Monitor (FIM)

File Integrity Monitor (FIM) — десктоп-приложение на C++ для анализа  файлов и контроля их целостности.
Программа позволяет загружать произвольные файлы, вычислять их криптографический хеш (SHA-256), подписывать метаданные с помощью HMAC.
Приложение разработано для Linux, использует Qt 6 (Qt Widgets) для графического интерфейса и распространяется в формате AppImage.


.


🕓 История сканирований

В базе данных хранится:

список загруженных файлов,

дата и время сканирования,

SHA-256 хеш,

статус целостности,

комментарии.

🔏 Поддержка HMAC

Метаданные файлов подписываются HMAC (SHA-256).
Это позволяет обнаружить подмену или модификацию информации в базе данных.
Запись считается корректной, если подпись совпадает при повторной проверке.

🧩 Архитектура и структура проекта

Проект построен по модульному принципу, что упрощает сопровождение и расширение функциональности.

FIM/

├── core/        # Ядро приложения

├── storage/     # Работа с базой данных

├── gui/         # Графический интерфейс (Qt)

└── main.cpp     # Точка входа

🧠 Ядро приложения (core/)

FileIntegrityEngine
Центральный компонент ядра. Управляет процессом сканирования, проверкой подписи, взаимодействием с хешером и хранилищем.

FileScanner
Выполняет постраничное чтение файлов и формирует список метаданных.

FileMetadata
Структура с метаинформацией:

путь,

размер,

владелец,

права доступа и др.

FileStatus
Перечисление состояний файла: Ok, Changed, Error и др.

IHasher
Абстрактный интерфейс для хеширования.

ScanSummary
Краткий отчёт о результатах сканирования.

🗄 Работа с базой данных (storage/)

DatabaseManager
Управляет соединением с SQLite:

создаёт таблицы при первом запуске,

контролирует версию схемы,

выполняет миграции,

обеспечивает атомарность операций.

Таблицы:

files — текущие данные о файлах,

scan_history — история изменений.

QtStorageAdapter
Адаптер между IStorage и DatabaseManager.
Используется для хранения истории и пользовательских настроек интерфейса.

🖥 Графический интерфейс (gui/)

MainWindow — главное окно приложения

FileMonitor — управление процессом сканирования

ScanWorker — выполнение сканирования в отдельном потоке (QThread)

Notifier — система уведомлений (tray / e-mail)

QtHasher — реализация SHA-256 через QCryptographicHash
🗄 Работа с базой данных (storage/)

    DatabaseManager
    Управляет соединением с SQLite:

        создаёт таблицы при первом запуске,

        контролирует версию схемы,

        выполняет миграции,

        обеспечивает атомарность операций.

    Таблицы:

        files — текущие данные о файлах,

        scan_history — история изменений.

    QtStorageAdapter
    Адаптер между IStorage и DatabaseManager.
    Используется для хранения истории и пользовательских настроек интерфейса.

🖥 Графический интерфейс (gui/)

    MainWindow — главное окно приложения

    FileMonitor — управление процессом сканирования

    ScanWorker — выполнение сканирования в отдельном потоке (QThread)

    Notifier — система уведомлений (tray)

    QtHasher — реализация SHA-256 через QCryptographicHash

📦 Зависимости

    C++17

    Qt 6 (Qt Widgets)

    SQLite

    QCryptographicHash / OpenSSL (HMAC SHA-256)

    CMake

    AppImageTool

Использование AppImage

Устновить последнюю версию во вкладке Release
Выдать приложению права
chmod +x FIM-x.y.z.AppImage
./FIM-x.y.z.AppImage

▶ Использование приложения

    Открытие файла
    Файл → Открыть файл…

    Hex Viewer
    Просмотр и анализ байтов файла.

    Heatmap View
    Настройка плотности и обновление визуализации.

    Audio View
    Воспроизведение бинарных данных как аудио.

    File Info
    Общая информация о файле.

    Настройки
    Меню → Настройки — изменения сохраняются автоматически.

    История
    История сканирований отображается и восстанавливается при следующем запуске.



